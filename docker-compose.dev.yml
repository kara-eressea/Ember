# Dev-only services. Apps run on the host (HMR); this provides Postgres.
# fchat-sim runs on the host too for now (`pnpm --filter @emberline/fchat-sim start`);
# it joins this file when the prod image exists (M1 step 11).
#
# Credentials below are local-dev fixtures, never used in production.
services:
  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_USER: emberline
      POSTGRES_PASSWORD: emberline
      POSTGRES_DB: emberline
    ports:
      - "5432:5432"
    volumes:
      # postgres:18 images moved the volume mount up to /var/lib/postgresql
      - pgdata:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U emberline"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  pgdata:
